services:
  dhan:
    build: .
    container_name: dhan-trading
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /run
      - /home/appuser:size=128m,mode=1777
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    security_opt:
      - no-new-privileges:true
    ports:
      # Bound to loopback only — reach via host reverse proxy (Caddy/nginx).
      # JupyterLab on 8888 must go through forward_auth on /lab/*.
      - "127.0.0.1:8000:8000"
      - "127.0.0.1:8888:8888"
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Kolkata}
    volumes:
      - config:/config
      - data:/app/data
      - notebooks:/app/notebooks

volumes:
  config:
  data:
  notebooks:
